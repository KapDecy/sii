# 🚲 Парковочная система учета транспорта (YOLOv8 + Streamlit)

Проект разработан в рамках учебной практики. Представляет собой веб-приложение для автоматического детектирования и подсчета транспортных средств (велосипедов, автомобилей, мотоциклов) на парковках с использованием компьютерного зрения.

Система использует архитектуру **YOLOv8** для детекции объектов и фреймворк **Streamlit** для визуализации результатов. Оптимизировано для работы на ОС Linux с поддержкой аппаратного ускорения на видеокартах **AMD (ROCm)**.

## 📋 Функциональные возможности

* 📷 **Мультиформатность:** Поддержка загрузки изображений (JPG, PNG) и видеофайлов (MP4, AVI, MOV).
* 🧠 **Умная фильтрация:** Игнорирование нецелевых объектов (пешеходов, животных), учет только транспортных средств.
* 📊 **Статистика:** Подсчет количества объектов в кадре в реальном времени.
* 💾 **История:** Автоматическое сохранение результатов в локальную базу данных (SQLite).
* 📈 **Отчетность:** Генерация и выгрузка отчетов в формате CSV/Excel.
* ⏯️ **Контроль:** Функция паузы/запуска обработки видео.

## 🛠️ Системные требования

* **ОС:** Linux (рекомендуется Arch Linux / Manjaro / Ubuntu)
* **Python:** 3.10 или выше
* **Package Manager:** `uv` (рекомендуется) или `pip`
* **GPU:** AMD Radeon (с поддержкой ROCm) или NVIDIA (CUDA)

## ⚙️ Установка и настройка

### 1. Клонирование репозитория
```bash
git clone [https://github.com/your-username/parking-detection.git](https://github.com/your-username/parking-detection.git)
cd parking-detection


### 2. Создание виртуального окружения (через uv)

```bash
uv venv
source .venv/bin/activate

```

### 3. Установка зависимостей

**Важно для пользователей AMD:** PyTorch необходимо устанавливать из специального индекса ROCm.

```bash
# 1. Установка базовых библиотек
uv pip install streamlit opencv-python-headless pandas ultralytics

# 2. Установка PyTorch с поддержкой ROCm (версия 6.2)
# Если у вас NVIDIA, уберите флаг --index-url
uv pip install torch torchvision torchaudio --index-url [https://download.pytorch.org/whl/rocm6.2](https://download.pytorch.org/whl/rocm6.2)

```

## 🚀 Использование

### Запуск веб-приложения

Для видеокарт AMD Radeon (серии RX 6000, 7000 и новее) часто требуется принудительно указать версию архитектуры через переменную `HSA_OVERRIDE`.

**Команда для запуска:**

```bash
HSA_OVERRIDE_GFX_VERSION=10.3.0 uv run streamlit run app.py

```

*(Если у вас NVIDIA или только CPU, переменную `HSA_OVERRIDE...` писать не нужно).*

Приложение будет доступно в браузере по адресу: `http://localhost:8501`

### Обучение модели (Опционально)

Если вы хотите дообучить модель на своем датасете:

1. Поместите датасет в папку `dataset/` (структура YOLOv8).
2. Настройте `dataset/data.yaml`.
3. Запустите скрипт обучения:
```bash
HSA_OVERRIDE_GFX_VERSION=10.3.0 uv run python train.py

```



## 📁 Структура проекта

```text
.
├── app.py               # Основной файл приложения (Streamlit + Logic)
├── train.py             # Скрипт для запуска обучения (Training loop)
├── requirements.txt     # Список зависимостей (для справки)
├── parking_history.db   # Локальная база данных (генерируется автоматически)
├── dataset/             # Папка для датасетов (data.yaml, train, valid)
├── runs/                # Папка с результатами обучения и весами моделей
└── README.md            # Документация проекта

```
